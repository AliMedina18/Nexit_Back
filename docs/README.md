# Documentación de Nexit — índice por fase

Este índice existe para que cualquiera (incluida una futura sesión) pueda retomar el proyecto sin tener que adivinar en qué orden leer los documentos. Cada archivo lleva un número que refleja el orden en que se hicieron las cosas, no necesariamente el orden en que conviene leerlas si solo quieres entender "dónde estamos hoy" — para eso, empieza por el resumen de abajo y entra al documento que te interesa.

## Resumen de dónde estamos

1. Se analizó a fondo la operación real de Next (Excel históricos + prototipo HTML) y se diseñó el modelo de datos → **Fase 1, completa.**
2. Se diseñó y construyó el backend (Clean Architecture, .NET 8): las 4 capas, los controladores, autenticación JWT, autorización → **Fase 2 (construcción del backend), completa — ver los documentos 02 y 03.**
3. Se auditó ese backend (funcionalidad + seguridad) y se aplicaron las correcciones que no dependen de que exista todavía un proyecto de Supabase real → **hecho el 2026-08-17, ver el documento 04 y el 05.**
4. Se diseñó e implementó el modelo de permisos de 4 niveles (super_admin/admin/manager/miembro), con el flujo de solicitudes de eliminación para clientes/proveedores/proyectos → **hecho el 2026-08-18, ver el documento 06.**
5. Se construyó la lógica de backend del calendario de proyectos (por mes/año, eficiente), se restringieron los informes a solo super_admin/admin, y se agregó exportación a Excel de informes → **hecho el 2026-08-18, ver el documento 07.**
6. Se agregó la capa de pruebas funcionales (Postgres real con Testcontainers, encontró y corrigió un bug real de guardado) y una capa de pruebas de seguridad (autorización por defecto, límite de peticiones, inyección SQL, mass assignment, autorización a nivel de objeto), y se eliminó una dependencia vulnerable sin usar (AutoMapper) → **hecho el 2026-08-18, ver el documento 08.**
7. Se agregó la validación de dominio de correo también en la aplicación (no solo en el trigger de Postgres) al registrar un usuario, y se documentó paso a paso cómo crear el proyecto de Supabase (cuenta gratuita ahora, y cómo repetirlo con plan Pro y correo corporativo cuando esté listo), incluida la cuenta del primer super_admin y el registro de la segunda cuenta real (Yuliana Navarro, admin) → **hecho el 2026-08-20, ver el documento 09.**
8. Se documentó a detalle, para arrancar el frontend (que vive en otro repositorio), qué hace y qué no hace el backend con correos y autenticación: todo el envío real de correos (invitaciones, códigos de acceso) lo hace Supabase Auth directamente, no este backend; y qué falta (invitar+registrar en un solo paso, notificaciones de solicitudes de eliminación, recuperar contraseña) → **hecho el 2026-08-21, ver el documento 10.**
9. Se documentó, también para el frontend, el recorrido completo del sistema módulo por módulo (clientes, proveedores + adjuntos, proyectos + equipo + seguimiento, calendario, catálogos, informes, usuarios, solicitudes de eliminación): campos exactos, reglas de validación, permisos por rol en cada endpoint, y comportamientos transversales (reemplazo completo de colecciones hijas en cada PUT, sin paginación/búsqueda server-side, formato de errores) → **hecho el 2026-08-21, ver el documento 11.**
10. Se cambió el diseño de login: ya no es "solo la super administradora tiene contraseña" — ahora **todos los roles** entran la primera vez con código OTP y ahí mismo crean su contraseña, para usarla de ahí en adelante (más un flujo de cambiarla/recuperarla más tarde). Se actualizó el documento 10 con el diseño nuevo y se empezó el backlog de historias de usuario (login como HU-01/HU-02, y qué ve cada rol como HU-03) → **hecho el 2026-08-21, ver el documento 12.**
11. Se decidió crear el proyecto de Supabase directo en plan **Pro** (no gratis-y-migrar-después como decía el documento 09 originalmente), dimensionado para hasta 25 personas, y conectar un proveedor de correo transaccional (Resend recomendado) **antes** de crear el proyecto, para que los códigos OTP funcionen desde el día uno sin el límite de 2 correos/hora del servidor de pruebas de Supabase → **hecho el 2026-08-21, ver el documento 13** (reordena y ajusta los pasos del documento 09, no lo reemplaza por completo).
12. Se creó el proyecto real de Supabase (Pro), se conectó `Nexit_Back` en producción contra él (Session pooler, rol `nexit_app` de mínimo privilegio), se corrigió un bug real de orden de ejecución de los scripts SQL, y se resolvieron las 5 advertencias "Function Search Path Mutable" del Security Advisor → **hecho el 2026-08-23, ver los documentos 09 y 13, y `schema/05`.**
13. Se diseñaron y aplicaron las tres plantillas de correo de autenticación con la identidad de marca de Nexit (login por código, restablecer contraseña, y la notificación de seguridad de contraseña cambiada), y se definió la política de contraseñas (mínimo 10 caracteres, mayúscula, minúscula, número y símbolo) → **hecho el 2026-08-23, ver los documentos 14, 15 y 16, y la sección 7 del documento 10.**
14. Se completó el módulo de usuarios: desactivar ahora bloquea el acceso de verdad (no solo lo marcaba en la base), y se agregó la eliminación automática tras 30 días desactivado, con respaldo previo en `usuarios_eliminados` — siguiendo el mismo patrón de otros productos (Microsoft Entra ID, GitLab, Facebook) → **hecho el 2026-08-23, ver el documento 17 y `schema/06`.**
15. Se agregó la historia de cerrar sesión (HU-07, 100% Supabase, sin código nuevo) y, en `docs/17`, la recomendación concreta para acortar la ventana de token válido tras desactivar/cerrar sesión (bajar el JWT expiry en el dashboard de Supabase) → **hecho el 2026-08-23, ver el documento 12 (HU-07) y la sección 6 del documento 17.**
16. Se corrigió el cálculo de mes/año del calendario de proyectos: ahora se decide por la hora LOCAL de la sede de cada proyecto (Bogotá o México, según `SedeNext`), no por UTC ni por la zona horaria de sesión del servidor — evita que un proyecto tarde en la noche cerca de fin de mes aparezca en el mes siguiente → **hecho el 2026-08-23, ver el documento 18.**
17. Se construyeron las tres piezas que faltaban del flujo de solicitudes de eliminación y de proveedores: notificaciones en bandeja (con la regla de que una sola decisión del administrador resuelve todas las solicitudes pendientes de la misma entidad), historial de cambios por campo para proyectos/proveedores/clientes (tipo Google Docs/Excel), y el marcado propio "trabajando con este proveedor" (público, muchos-a-muchos, alimenta la vista "mis proveedores") → **hecho el 2026-08-24, ver el documento 20 (HU-08 en el documento 12) y `schema/07`.**
18. Se investigó cómo otros sistemas resuelven "a qué proveedor/cliente/proyecto atender primero" (lead scoring de CRM, RFM, tiering de proveedores, next-best-action con IA, microagentes que leen notas de texto libre) y se dejó una propuesta concreta de 3 niveles usando datos que Nexit ya captura (Nivel 1: rúbrica de puntos transparente, sin IA; Nivel 2: microagente de IA opcional que lee notas/bitácora; Nivel 3: aprendizaje de casos pasados, a futuro) → **hecho el 2026-08-24, ver el documento 21.**
19. La usuaria confirmó empezar por el Nivel 1 (reglas, sin IA todavía) — se construyó y probó para proyectos: `GET /api/proyectos/prioridad` devuelve los proyectos activos puntuados de 0 a 100 con la lista de razones de cada puntaje (evento cercano, prioridad manual, sin actividad reciente, propuesta/brief pendientes, sin pagar con el evento cerca) → **hecho el 2026-08-24, ver el documento 22 (HU-09 en el documento 12).**
20. Se revisaron a fondo los dos proyectos de referencia que la usuaria descargó (`retail-supply-chain-analytics-main` y OmniSupply, un multi-agente de IA de verdad): ningún código es reutilizable (Python, y OmniSupply además tiene licencia GPLv3 pese a que el README dice MIT), pero se rescataron dos ideas de diseño y una validación clave — incluso el sistema de IA calcula el puntaje de riesgo con aritmética 100% determinística por reglas, y solo usa el modelo de lenguaje para redactar el resumen, el mismo patrón que ya se eligió para Nexit → **hecho el 2026-08-24, ver el documento 23.**
21. La usuaria confirmó que no se va a usar IA (sin presupuesto para eso) pero sí quiere la misma lógica de esos dos proyectos, traducida a C#/.NET — se extendió la rúbrica de puntos de `docs/22` a proveedores (`GET /api/proveedores/prioridad`: Score bajo, sin calificar pero ya usado, buen proveedor sin uso reciente, colaboradores sin proyecto formal) y a clientes (`GET /api/clientes/prioridad`: recencia y frecuencia, tipo RFM) → **hecho el 2026-08-24, ver el documento 24 (HU-10 en el documento 12).**
22. Se construyó el único hueco real de backend que quedó al revisar "qué falta" (`docs/10`, sección 5): invitar y registrar a alguien del equipo en un solo paso desde dentro de Nexit, con la persona invitada aceptando o rechazando ella misma (con un mensaje de quien invita) la primera vez que entra → **hecho el 2026-08-24, ver el documento 25 (HU-11 en el documento 12).**
23. Se registraron dos cuentas nuevas de administrador pendientes de invitar (`administracion@agencianextmkt.com`, `andresacuna@agencianextmkt.com`, rol `admin`) y se documentó — sin construir todavía, quedan preguntas de diseño abiertas — la propuesta de presencia en vivo para administradores ("quién tiene el sistema abierto ahora mismo", tipo Microsoft Teams), pedida por WhatsApp → **hecho el 2026-08-25, ver el documento 26 (HU-12 en el documento 12).**
24. Se configuró `Supabase:ProjectUrl`/`Supabase:ServiceRoleKey` en `appsettings.Production.json` (contra el proyecto real), se activó el Auth Hook, y se encontró y corrigió un bug real de producción: Row Level Security bloqueaba la lectura del rol de cualquier persona dentro del propio Auth Hook (por un GRANT de tabla sin la política de RLS correspondiente para `supabase_auth_admin`), así que **nadie** — ni la super administradora real — habría podido pasar ninguna verificación de rol hasta corregirlo. Se confirmó SMTP de Gmail ya conectado. Se decidió posponer el envío de invitaciones reales hasta tener el backend desplegado → **hecho el 2026-08-25, ver el documento 27 y `schema/09_fix_rls_auth_hook.sql`.**
25. Pendiente: elegir dónde desplegar el backend (candidatos evaluados con costo real: Azure B1 ~US$13.14/mes ≈ $40,300 COP/mes para 20-25 personas, y candidatos gratuitos Azure App Service F1/Render free con sus limitaciones — ver la conversación de esta sesión), completar `ForwardedHeaders:KnownProxies`/`KnownNetworks` una vez elegido, definir el Site URL/Redirect URLs de Supabase cuando el frontend esté más avanzado, disparar las tres invitaciones pendientes (Yuliana, `administracion@`, `andresacuna@`) una vez haya sistema desplegado (con el texto del correo ya rediseñado para no prometer acceso inmediato), avanzar con el frontend siguiendo el backlog de historias de usuario del documento 12, con el detalle de cada módulo en el documento 11. También pendiente, sin backend todavía por faltar más definición (no es un factor externo): qué mejorar específicamente del informe semanal (ver documento 20), ajustar los pesos de las tres rúbricas de prioridad con casos reales (ver documentos 22 y 24), y las preguntas de diseño abiertas de la presencia en vivo (ver documento 26).
26. Se construyó la subida real de archivos adjuntos de proveedores (PDF/Excel) a Supabase Storage — bucket privado con límite de 20MB y validación de tipo en dos capas, endpoints de subir/descargar (URL firmada temporal), borrado en cascada del archivo al eliminar el adjunto, y la aclaración de que la base de datos nunca guarda los bytes del archivo (solo la referencia), por lo que el volumen de archivos no afecta su rendimiento. Migración aplicada a `nexit_dev` y compilación/pruebas confirmadas por la usuaria (234 pruebas, 8 nuevas, cero regresiones) → **hecho el 2026-08-25, ver el documento 28 (HU-13 en el documento 12).**
27. Se cerraron las tres últimas historias de usuario que dependían de pasos manuales en Supabase: **HU-06** (desactivación/eliminación automática) ya estaba resuelta — se verificó directamente contra el proyecto real que los scripts ya habían corrido, sin necesitar ninguna acción — y **HU-04/HU-05** (recuperar contraseña y aviso de contraseña cambiada) se cerraron con una prueba real de punta a punta: se disparó un `resetPasswordForEmail` real contra `analistacompras@agencianextmkt.com`, se verificó el código de recuperación, se confirmó que las dos plantillas de correo (ya pegadas por la usuaria) llegan bien formateadas, se confirmó la política de contraseña completa activa en el dashboard, y se completó un cambio de contraseña real que disparó el correo de aviso de seguridad — con esto, la contraseña real de esa cuenta quedó actualizada. Con esto, **todas las historias de usuario documentadas en el backend (HU-01 a HU-13) quedan en ✅**, salvo la presencia en vivo (HU-12), que sigue en 🔴 a la espera de decisiones de diseño. Lo único pendiente del sistema completo es el despliegue del backend y avanzar con el frontend (ver el punto 25) → **hecho el 2026-08-25.**
28. Se investigó cómo otros sistemas resuelven la presencia en vivo (heartbeat + umbral de desconexión, y la función nativa de Supabase Realtime Presence como alternativa) y se construyó HU-12 con un heartbeat propio en `Nexit_Back` — `POST /api/presencia/ping` (cualquier autenticado) y `GET /api/presencia` (solo admin/super_admin, directorio con quién está en línea según el último ping, umbral configurable de 2 minutos por defecto) — se prefirió esto sobre Supabase Realtime Presence para mantener la autorización por rol consistente con el resto del backend, sin la complejidad extra de RLS sobre `realtime.messages`. Código y 9 pruebas nuevas listas, pendiente compilar/probar y aplicar la migración a `nexit_dev` (mismo paso pendiente que en el punto 26) → **hecho el 2026-08-25, ver el documento 29 (HU-12 en el documento 12, `docs/26` queda como referencia histórica del pedido original).**
29. Al construir el punto 28 apareció un bug real de compilación (un caso de uso de `Nexit.Application` referenció por accidente un tipo de Entity Framework Core, algo que esa capa nunca debe hacer en Clean Architecture) — se corrigió, y además se usó ese bug real como motivo para investigar y agregar tipos de prueba que faltaban en el backend: **pruebas de arquitectura** (`NetArchTest.Rules`, nuevas, verifican solas que la separación entre Core/Application/Infrastructure/API se mantenga — habrían detectado ese bug automáticamente), **reporte de cobertura de código** navegable (`dotnet-reportgenerator-globaltool`, con un script listo `scripts/generar-reporte-cobertura.ps1`), y **pruebas de mutación** documentadas como chequeo manual opcional (`Stryker.NET`, no integradas a cada `dotnet test` por ser lentas). También se confirmó que la exportación a Excel de informes (`docs/07`) ya tenía pruebas reales desde antes (abren el `.xlsx` generado y verifican su contenido, no solo que se produjeron bytes). Todo documentado en `docs/08-tipos-de-pruebas.md` → **hecho el 2026-08-25.**
30. Se cerró HU-12 (presencia en vivo) de verdad: se generó la migración `AddPresenciaUsuarios` que faltaba (nunca se había creado el archivo, por eso las pruebas fallaban con "column ultima_actividad does not exist"), se corrigió un defecto real de diseño en la prueba de borde del umbral (comparaba contra `DateTime.UtcNow` calculado en dos momentos distintos, así que nunca podía dar exactamente igual — se le agregó un margen de 500 ms), se aplicó la migración a `nexit_dev`, y se confirmó la compilación limpia y 251 pruebas en total (244 pasan, 7 dependen de Docker en este entorno — nada nuevo, mismo patrón que en el punto 26). Con esto, **HU-12 pasa a ✅** y las 13 historias de usuario documentadas del backend (HU-01 a HU-13) quedan completas. Queda listo `schema/11_presencia_usuarios.sql` para producción → **hecho el 2026-08-26, ver el documento 12 y `schema/11`.**
31. Al conectar el frontend contra todo el backend se encontraron dos huecos reales: no había forma de listar la bitácora completa de seguimiento de un proyecto (solo agregar una nota nueva), y `UsuariosController` era `SuperAdminOnly` completo, así que nadie fuera de super_admin podía ver su propio perfil (nombre/apellido/rol reales, más allá del rol que ya traía el JWT). Se agregó `GET /api/proyectos/{id}/seguimiento` y `GET /api/usuarios/me` (única excepción a `SuperAdminOnly` en ese controlador), ambos con pruebas nuevas (unitarias para seguimiento, funcionales para `/me`, incluida la de que un miembro no puede listar a todos ni ver el perfil de otra persona), y se conectó el lado del frontend (`usuariosApi.me()`, `proyectosApi.listarSeguimiento()`, y `auth-store.ts` ahora completa el perfil aproximado del correo con el nombre real apenas responde `/me`) → **hecho el 2026-08-26, ver el documento 11 (secciones 1, 4.1 y 8) y el documento 12 (HU-03).**
32. Se ajustó el modelo de permisos de `usuarios` de nuevo, esta vez pidiendo la usuaria explícitamente que *ver* sea más abierto que *gestionar*: `admin` (no solo `super_admin`) ya puede pedir el directorio completo (`GET /api/usuarios`, política `AdminOrAbove`), y cualquier persona autenticada puede ver el perfil individual de un compañero (`GET /api/usuarios/{id}`, antes exclusivo de super_admin) -- de solo lectura, sin poder editar ni eliminar, como el directorio de Microsoft Teams. Crear/editar/eliminar sigue siendo `SuperAdminOnly`. Se actualizaron las pruebas de autorización (unitarias/integración) y se agregaron 4 pruebas funcionales nuevas (admin lista a todos; un miembro ve el perfil de otro pero no puede editarlo ni eliminarlo) → **hecho el 2026-08-26, ver el documento 06 (secciones 1, 3 y 6) y el documento 11 (secciones 1 y 8).**
33. Se construyó la detección automática de "primera vez vs. recurrente" en el login: en vez de que la persona elija a mano entre código o contraseña, `GET /api/auth/estado-cuenta?email=...` (único endpoint público de toda la API, límite de tasa propio por IP) le dice al frontend si ese correo ya tiene contraseña configurada -- respuesta idéntica tanto si el correo no existe como si existe pero aún no tiene contraseña, para no poder enumerar cuentas. La marca (`usuarios.contrasena_configurada`) la pone el propio frontend justo después de que Supabase confirma que la contraseña quedó guardada (`POST /api/auth/confirmar-contrasena`, best-effort). Ya conectado en `Nexit_Front` (login), con 269 pruebas en total (252 sin Docker) y las 74 de Security/Auth completas → **hecho el 2026-09-01, ver el documento 30 y `schema/12`.** *(No estaba indexado en este README hasta ahora -- ver el punto 34.)*
34. Se agregó CI real (GitHub Actions) a los dos repositorios -- `ci.yml` corre en cada push/PR a `main` (frontend: lint, tipos, auditoría npm, pruebas unitarias, build, pruebas E2E con Playwright; backend: restaura, compila en Release, auditoría de paquetes NuGet vulnerables, toda la suite de pruebas incluidas las funcionales con Testcontainers) más `dependabot.yml` en ambos -- y se cerró el único hueco real que quedaba en "la parte del bucket" (Storage de adjuntos de proveedor, documento 28): su configuración (privado, 20 MB, whitelist de MIME) vivía solo en el dashboard sin quedar en ningún script; ahora es un UPSERT idempotente (`schema/13_bucket_adjuntos_proveedores.sql`, probado localmente contra un Postgres real). Se agregaron también las pruebas unitarias que le faltaban al servicio del frontend que habla con ese endpoint (`proveedor-adjuntos-service.test.ts`). Sigue pendiente, y solo la puede hacer la usuaria: correr `schema/10_adjuntos_content_type_tamano.sql` y `schema/13` contra el Supabase real de producción (ver la sección "Pendientes" del documento 28) → **hecho el 2026-09-02.**
35. Se cerró el resto de huecos de "workflow" que quedaban del punto 34: se agregaron las pruebas unitarias que le faltaban a `auth-service.ts` (el servicio de frontend de la detección "primera vez vs. recurrente" del punto 33, `auth-service.test.ts`), se indexaron en este README el documento 30 y `schema/12` (existían y estaban aplicados a `nexit_dev`, pero nunca habían quedado en esta tabla — ver el punto 33), y se le agregó `IF NOT EXISTS` a las tres migraciones de columna que todavía no lo tenían (`schema/10`, `schema/11`, `schema/12` — mismo patrón que ya usaban `schema/06`/`schema/07`/`schema/13`), probado corriendo cada una dos veces seguidas contra un Postgres real: la primera vez agrega la columna, la segunda no falla, solo no hace nada — así que de ahora en adelante es seguro re-correr cualquier script de producción sin tener que confirmar antes si ya se aplicó. Con esto, **todo el "workflow" de código, pruebas y CI queda completo** — lo único que falta para que producción esté al día son los pasos de base de datos listados abajo, y esos solo los puede correr la usuaria → **hecho el 2026-09-02.**
36. Antes de que la usuaria empezara a probar el sistema completo localmente como super_admin, pidió (comparando con un sistema anterior, GAMO) un botón para exportar e importar datos de Clientes, Proveedores y Proyectos en Excel — de paso se aclaró que no existe (ni existió) exportación a PDF en ningún lugar del sistema, solo Excel. Se construyó exportar/importar simétrico para las tres entidades: `GET .../exportar` y `POST .../importar` (`admin`/`super_admin`, multipart) en los tres controladores, reutilizando los mismos casos de uso y validadores que crear un registro uno por uno (nunca hay dos formas distintas de decidir si un dato es válido); cada fila se procesa de forma independiente (una fila mala no detiene el resto, queda reportada con su número y motivo), la importación SIEMPRE crea (nunca actualiza — no hay llave natural confiable), y país/ciudad/categoría/estado se resuelven por nombre contra Catálogos sin auto-crear ni adivinar el más parecido. El Excel exportado sirve como su propia plantilla para reimportar. Conectado también en `Nexit_Front` con un solo componente reutilizado (`ImportExportBar.tsx`) en las tres pantallas, reemplazando el export CSV local que tenía antes la pantalla de Proyectos. Diseño completo, columnas exactas de cada entidad y por qué el import de proyectos deja fuera equipo/proveedores/gerente → **hecho el 2026-09-02, ver el documento 31.**

### Qué falta correr contra el Supabase real de producción (para que quede al día)

Estado verificado leyendo los propios archivos (no solo lo que dicen los documentos) — de los 13 scripts de `schema/`, esto es lo que consta como pendiente o sin confirmar:

| Script | Estado | Qué hace |
|---|---|---|
| `schema/01` a `schema/05` | ✅ Aplicados — el proyecto real de Supabase se creó y configuró con ellos (punto 12). | Esquema base, rol de mínimo privilegio, Auth Hook, extras de RLS, fix de Function Search Path. |
| `schema/06` | ✅ Aplicado — verificado directamente contra el proyecto real (punto 27, HU-06). | Desactivación/eliminación automática de usuarios. |
| `schema/09` | ✅ Aplicado — el propio archivo dice "corregido en producción el 2026-08-25" (documento 27). | Fix de RLS que le faltaba al Auth Hook. |
| `schema/07` | ⚠️ No confirmado por escrito en ningún documento. Es `IF NOT EXISTS` (seguro re-correrlo aunque ya esté). | Notificaciones, historial de cambios, "trabajando con este proveedor". |
| `schema/08` | ⚠️ No confirmado por escrito. Es `IF NOT EXISTS` (seguro re-correrlo aunque ya esté). | Invitaciones de equipo. |
| **`schema/10`** | 🔴 **Pendiente, confirmado** — solo aplicada a `nexit_dev` (documento 28). | Columnas `content_type`/`tamano_bytes` de adjuntos — sin esto, subir un archivo real falla en producción. |
| **`schema/11`** | 🔴 **Pendiente, confirmado** — solo aplicada a `nexit_dev` (documento 29). | Columna `ultima_actividad` — sin esto, presencia en vivo falla en producción. |
| **`schema/12`** | 🔴 **Pendiente, confirmado** — solo aplicada a `nexit_dev` (documento 30). | Columna `contrasena_configurada` — sin esto, el login recién construido (punto 33) falla en producción. |
| **`schema/13`** | 🔴 Pendiente, pero no urgente — el bucket ya existe y ya funciona; esto solo lo deja scripteado para el futuro. | Configuración del bucket `adjuntos-proveedores`. |

**Para no tener que adivinar `schema/07`/`schema/08`**: en el Table Editor de Supabase, revisa si ya existen las tablas `notificaciones`/`historial_cambios`/`proveedor_colaboradores` (07) e `invitaciones_equipo` (08) — si ya existen, no pasa nada al re-correr esos scripts (son `IF NOT EXISTS`); si no existen, hace falta correrlos.

**Cómo correr los 4 pendientes reales (10, 11, 12, 13):** en el SQL Editor del dashboard de Supabase del proyecto real, pega y ejecuta el contenido de cada archivo — sin orden fijo entre ellos, ninguno depende de otro. Los 4 son ahora seguros de re-correr por accidente (no fallan si ya se aplicaron).

## Documentos

| # | Documento | Qué contiene |
|---|---|---|
| 01 | [`01-analisis-fase1.md`](01-analisis-fase1.md) | Análisis de la operación real de Next, comparación contra el prototipo de Luisa, y el modelo de datos completo (20 tablas, normalizado 1FN-4FN, con el modelo relacional formal). |
| 02 | [`02-diseno-arquitectura-backend.md`](02-diseno-arquitectura-backend.md) | Diseño de la base arquitectónica del backend: Clean Architecture en 4 proyectos .NET (Core/Application/Infrastructure/API), stack tecnológico, y las decisiones clave (autenticación, mapeo, validación). |
| 03 | [`03-plan-implementacion-backend.md`](03-plan-implementacion-backend.md) | Plan de implementación tarea por tarea de esa base arquitectónica (creación de proyectos, entidades, DTOs, validadores, casos de uso, repositorios, DbContext) — el paso a paso que se siguió para construir el backend. |
| 04 | [`04-auditoria-seguridad-backend.md`](04-auditoria-seguridad-backend.md) | Auditoría de funcionalidad y seguridad del backend ya construido: qué compila, qué pasa en pruebas, y 12 hallazgos priorizados (H1-H12). |
| 05 | [`05-plan-remediacion-seguridad.md`](05-plan-remediacion-seguridad.md) | Qué se corrigió de esos 12 hallazgos, cómo, qué queda pendiente y por qué (casi todo lo pendiente depende de que exista un proyecto de Supabase real), más la investigación de APIs públicas para almacenamiento de adjuntos y geolocalización. |
| 06 | [`06-modelo-permisos-roles.md`](06-modelo-permisos-roles.md) | Modelo de permisos de 4 niveles (super_admin/admin/manager/miembro): matriz de permisos, el concepto de gerente "dueño" de un proyecto, y el flujo de solicitudes de eliminación para clientes/proveedores/proyectos. |
| 07 | [`07-calendario-e-informes-excel.md`](07-calendario-e-informes-excel.md) | Backend del calendario de proyectos (por mes/año, agregación eficiente sin cargar entidades completas), restricción de Informes a solo super_admin/admin, y exportación a Excel de informes con ClosedXML. |
| 08 | [`08-tipos-de-pruebas.md`](08-tipos-de-pruebas.md) | La pirámide de pruebas completa: unitarias, integración, funcionales (Postgres real con Testcontainers, incluye el bug real que encontró) y seguridad (autorización por defecto, límite de peticiones, inyección, mass assignment, BOLA), más el hallazgo y limpieza de una dependencia vulnerable sin usar. |
| 09 | [`09-crear-proyecto-supabase-paso-a-paso.md`](09-crear-proyecto-supabase-paso-a-paso.md) | Guía paso a paso para crear el proyecto de Supabase (cuenta gratuita ahora, y cómo repetirlo con plan Pro y correo corporativo después): esquema, rol de mínimo privilegio, hook de roles, invitar a los dos primeros usuarios reales, el "bootstrap" del primer super_admin, y el diseño de login (contraseña solo para super_admin, código por correo para el resto). |
| 10 | [`10-correos-autenticacion-y-guia-frontend.md`](10-correos-autenticacion-y-guia-frontend.md) | Para arrancar el frontend (otro repositorio): qué hace y qué no hace el backend con correos — todo el envío real (invitaciones, códigos de acceso) lo hace Supabase Auth directamente, este backend nunca envía un correo — los dos flujos existentes, cómo se valida el login sin correo de por medio, y lo que falta (invitar+registrar en un solo paso, notificaciones de solicitudes de eliminación, recuperar contraseña). |
| 11 | [`11-flujo-completo-del-sistema-guia-frontend.md`](11-flujo-completo-del-sistema-guia-frontend.md) | Para arrancar el frontend: el recorrido completo del sistema módulo por módulo con el detalle exacto que ya está construido — clientes, proveedores (+ adjuntos), proyectos (+ equipo + bitácora de seguimiento + gerente responsable), calendario, catálogos, informes, usuarios, y el flujo de solicitudes de eliminación; más comportamientos transversales (reemplazo completo de colecciones hijas en cada PUT, sin paginación/búsqueda del lado del servidor, formato de errores). |
| 12 | [`12-historias-de-usuario.md`](12-historias-de-usuario.md) | Backlog vivo de historias de usuario, empezando por el login (HU-01 primera vez con código + crear contraseña, HU-02 ingreso recurrente con contraseña) y qué ve cada rol en el menú principal (HU-03, a nivel superficial) — con el diseño de login actualizado (ya no solo la super administradora tiene contraseña). Se va ampliando con una historia por cada módulo de `docs/11`. |
| 13 | [`13-correo-transaccional-y-supabase-pro.md`](13-correo-transaccional-y-supabase-pro.md) | Elegir y configurar un proveedor de correo transaccional para los códigos OTP, y crear el proyecto de Supabase directo en plan Pro (no gratis-y-migrar-después) dimensionado para hasta 25 personas — con el correo conectado ANTES de invitar a nadie. Reordena y ajusta los pasos del documento 09; qué información pasarle de vuelta al backend cuando esté listo. |
| 14 | [`14-plantilla-correo-otp.md`](14-plantilla-correo-otp.md) | Plantilla de correo con el diseño de marca de Nexit (color `#6fceca`, sin emojis, ícono en CSS puro) para el código de login (HU-01/HU-02) — incluye la nota técnica sobre por qué un ícono embebido en base64 se rompe en Gmail. |
| 15 | [`15-plantilla-correo-restablecer-contrasena.md`](15-plantilla-correo-restablecer-contrasena.md) | Misma plantilla adaptada a la recuperación de contraseña (HU-04) — plantilla separada dentro de Supabase ("Reset Password"), texto distinto. |
| 16 | [`16-plantilla-correo-contrasena-cambiada.md`](16-plantilla-correo-contrasena-cambiada.md) | Plantilla de la notificación de seguridad "Password changed" (HU-05) — se dispara sola cuando una contraseña cambia de verdad, sin código, con un llamado a la acción si la persona no reconoce el cambio. |
| 17 | [`17-eliminacion-automatica-usuarios.md`](17-eliminacion-automatica-usuarios.md) | Diseño de la eliminación automática de usuarios inactivos (HU-06): desactivar ahora bloquea el acceso de verdad, y tras 30 días desactivado el sistema elimina solo, con respaldo previo en `usuarios_eliminados` — incluye la investigación de cómo lo resuelven Microsoft Entra ID, GitLab y Facebook. Sección 6 incluye la recomendación de acortar el JWT expiry para desactivar/cerrar sesión (HU-07). |
| 18 | [`18-calendario-zona-horaria-por-sede.md`](18-calendario-zona-horaria-por-sede.md) | El mes/año de un proyecto en el calendario ahora se calcula por la hora local de su sede (Bogotá o México), no por UTC — corrige un bug real documentado (eventos tarde en la noche cerca de fin de mes quedaban en el mes siguiente), con la investigación que lo confirma y las pruebas nuevas. |
| 19 | [`19-diseno-notificaciones-historial-y-mis-proveedores.md`](19-diseno-notificaciones-historial-y-mis-proveedores.md) | Propuesta de diseño previa a construir la pieza del documento 20 — queda como referencia histórica; su sección de "mis proveedores" (opciones A/B) fue superada por lo que realmente se construyó (marcado propio, no asignado por un administrador), ver documento 20. |
| 20 | [`20-notificaciones-historial-y-colaboradores.md`](20-notificaciones-historial-y-colaboradores.md) | Lo que quedó construido y probado: notificaciones en bandeja para el flujo de solicitudes de eliminación (con la cascada de resolución de una sola decisión del administrador), historial de cambios por campo de proyectos/proveedores/clientes, y "trabajando con este proveedor" (marcado propio, público, muchos-a-muchos, alimenta "mis proveedores") — con los endpoints, el esquema nuevo y qué quedó fuera a propósito. |
| 21 | [`21-priorizacion-sugerencias-investigacion-y-propuesta.md`](21-priorizacion-sugerencias-investigacion-y-propuesta.md) | Investigación de cómo otros sistemas priorizan a quién atender primero (lead scoring, RFM, tiering de proveedores, next-best-action con IA, microagentes que leen notas) y propuesta concreta de 3 niveles para Nexit usando datos que ya existen. |
| 22 | [`22-sistema-prioridad-proyectos.md`](22-sistema-prioridad-proyectos.md) | Nivel 1 de la propuesta del documento 21, ya construido y probado: `GET /api/proyectos/prioridad`, la rúbrica de puntos (señales, pesos, por qué quedaron así) y los próximos pasos posibles (proveedores/clientes, el microagente de IA del Nivel 2, ajustar pesos con casos reales). |
| 23 | [`23-evaluacion-de-dos-proyectos-de-referencia.md`](23-evaluacion-de-dos-proyectos-de-referencia.md) | Evaluación honesta de los dos proyectos que la usuaria descargó y pidió revisar: qué es cada uno, por qué ningún código se puede reutilizar (Python vs. C#, y licencia GPLv3 real de OmniSupply pese al README diciendo MIT), y las dos ideas de diseño que sí valen la pena — incluida la validación de que el propio "sistema de IA" calcula su puntaje con reglas, no con el modelo de lenguaje. |
| 24 | [`24-prioridad-proveedores-y-clientes.md`](24-prioridad-proveedores-y-clientes.md) | La lógica de `docs/23` traducida a C#/.NET, sin IA: se extendió la rúbrica de puntos de `docs/22` a proveedores (`GET /api/proveedores/prioridad`) y clientes (`GET /api/clientes/prioridad`) — señales, pesos, por qué quedaron así, y los próximos ajustes posibles. |
| 25 | [`25-invitar-y-registrar-en-un-solo-paso.md`](25-invitar-y-registrar-en-un-solo-paso.md) | El único hueco real de backend identificado en `docs/10`, ya construido: `POST /api/invitaciones` dispara la invitación real por Supabase y registra el perfil en un solo paso; la persona invitada ve `GET /api/invitaciones/mia` y acepta o rechaza ella misma, con un mensaje de quien invita. |
| 26 | [`26-presencia-en-vivo-diseno.md`](26-presencia-en-vivo-diseno.md) | Propuesta (sin construir) de presencia en vivo para administradores — "quién tiene el sistema abierto ahora mismo", tipo Microsoft Teams — pedida por WhatsApp; deja las preguntas de diseño abiertas (mecanismo de detección, umbral de desconexión, quién puede verla) a confirmar antes de programar. |
| 27 | [`27-fix-rls-auth-hook-produccion.md`](27-fix-rls-auth-hook-produccion.md) | Bug real de producción encontrado y corregido: Row Level Security en `usuarios` bloqueaba la lectura del rol dentro del Auth Hook (el GRANT de tabla no basta con RLS activo), así que todo el mundo recibía el rol por defecto "miembro" sin importar su rol real — nadie podía pasar ninguna verificación de rol. Causa, corrección (`schema/09_fix_rls_auth_hook.sql`) y verificación. |
| 28 | [`28-subida-de-adjuntos-a-storage.md`](28-subida-de-adjuntos-a-storage.md) | Subida real de archivos (PDF/Excel) como adjuntos de proveedor a Supabase Storage — bucket privado, validación de tipo/tamaño en dos capas, URL de descarga firmada y temporal, borrado en cascada, y por qué esto no afecta el rendimiento de la base de datos (solo se guarda la referencia, nunca el archivo). Incluye la respuesta a la estimación de capacidad de la usuaria (HU-13 en el documento 12). |
| 29 | [`29-presencia-en-vivo-implementacion.md`](29-presencia-en-vivo-implementacion.md) | Resuelve las preguntas de diseño abiertas del documento 26: investigación de cómo lo resuelven otros sistemas (heartbeat + umbral vs. Supabase Realtime Presence), la decisión de construir un heartbeat propio y por qué, y el detalle de lo construido — `POST /api/presencia/ping`, `GET /api/presencia` (HU-12 en el documento 12). |
| 30 | [`30-deteccion-primera-vez-vs-recurrente.md`](30-deteccion-primera-vez-vs-recurrente.md) | Reemplaza la elección manual "primera vez / ya tengo contraseña" del login por una detección automática: `GET /api/auth/estado-cuenta` (único endpoint público de toda la API, con límite de tasa propio) y `POST /api/auth/confirmar-contrasena`, con la marca `usuarios.contrasena_configurada` puesta por el propio frontend — por qué no se le puede preguntar esto a Supabase directamente, y por qué la respuesta nunca revela si el correo existe. |
| 31 | [`31-importacion-exportacion-masiva.md`](31-importacion-exportacion-masiva.md) | Exportar/importar Clientes, Proveedores y Proyectos como Excel (`GET .../exportar`, `POST .../importar` con `admin`/`super_admin`): el mismo archivo sirve para exportar e importar, importar siempre crea (nunca actualiza), una fila mala no detiene el resto del archivo, y país/ciudad/categoría/estado se resuelven por nombre contra Catálogos sin auto-crear ni adivinar. Incluye por qué no hay ni hubo exportación a PDF, las columnas exactas de cada entidad, y por qué el import de proyectos deja fuera equipo/proveedores/gerente. |
| — | [`preguntas-permisos-roles-para-companera.md`](preguntas-permisos-roles-para-companera.md) | Preguntas preparadas para la compañera de equipo sobre el flujo de permisos — la usuaria terminó respondiéndolas ella misma directamente (ver documento 06), quedan aquí como referencia. |
| — | [`schema/01_esquema_completo.sql`](schema/01_esquema_completo.sql) | **Sí se ejecuta, primero (o vía `dotnet ef database update` en tu base local).** El esquema completo (26 tablas, columnas, llaves, índices, triggers) -- generado automáticamente desde las migraciones de EF Core (`dotnet ef migrations script`), no escrito a mano, así que no se puede desactualizar en silencio como pasó con `nexus_schema_v2.sql`. **Para Supabase, no lo corras tal cual si ya corriste `schema/06` a mano** — ver la nota dentro del propio archivo y usa `schema/07` y `schema/08` en su lugar para lo nuevo. |
| — | [`schema/02_rol_aplicacion_minimo_privilegio.sql`](schema/02_rol_aplicacion_minimo_privilegio.sql) | **Sí se ejecuta, segundo.** Crea el rol `nexit_app` de mínimo privilegio que usa el backend para conectarse (hallazgo H2). |
| — | [`schema/04_extras_supabase_post_migraciones.sql`](schema/04_extras_supabase_post_migraciones.sql) | **Sí se ejecuta, tercero** (después de `02`, antes de `03` — ver la nota de corrección de orden dentro del archivo). Lo único que las migraciones de EF Core no pueden crear porque depende del esquema `auth` de Supabase: la relación `usuarios.id` → `auth.users.id`, y Row Level Security. |
| — | [`schema/03_auth_hook_custom_claims.sql`](schema/03_auth_hook_custom_claims.sql) | **Sí se ejecuta, cuarto.** Auth Hook de Supabase que agrega el rol del usuario (`user_role`) y, desde HU-06, el claim `user_active` al JWT. |
| — | [`schema/seed_geografia_categorias_estados.sql`](schema/seed_geografia_categorias_estados.sql) | **Sí se ejecuta, quinto.** Datos iniciales de catálogos (países, regiones, ciudades, categorías de proveedor, estados de proyecto, dominios de correo permitidos). |
| — | [`schema/05_fix_search_path_funciones.sql`](schema/05_fix_search_path_funciones.sql) | **Sí se ejecuta, una vez, sin orden fijo.** Corrige la advertencia "Function Search Path Mutable" del Security Advisor en las 5 funciones del esquema. |
| — | [`schema/06_eliminacion_automatica_usuarios.sql`](schema/06_eliminacion_automatica_usuarios.sql) | **Sí se ejecuta, una vez, sin orden fijo.** Agrega la columna `fecha_desactivacion` y la tabla de respaldo `usuarios_eliminados` (HU-06, documento 17). |
| — | [`schema/07_notificaciones_historial_colaboradores.sql`](schema/07_notificaciones_historial_colaboradores.sql) | **Sí se ejecuta, una vez, sin orden fijo.** Crea `notificaciones`, `historial_cambios` y `proveedor_colaboradores`, con su RLS (HU-08, documento 20). |
| — | [`schema/08_invitaciones_equipo.sql`](schema/08_invitaciones_equipo.sql) | **Sí se ejecuta, una vez, sin orden fijo.** Crea `invitaciones_equipo`, con su RLS (HU-11, documento 25). |
| — | [`schema/09_fix_rls_auth_hook.sql`](schema/09_fix_rls_auth_hook.sql) | **Sí se ejecuta, una vez, sin orden fijo (pero antes de confiar en cualquier rol que traiga un JWT).** Agrega la política de RLS que le faltaba a `supabase_auth_admin` sobre `usuarios` — sin esto, el Auth Hook de `schema/03` no puede leer el rol de nadie (documento 27). |
| — | [`schema/10_adjuntos_content_type_tamano.sql`](schema/10_adjuntos_content_type_tamano.sql) | **Sí se ejecuta, una vez, sin orden fijo.** Agrega `content_type`/`tamano_bytes` a `proveedor_adjuntos` — la parte de producción de la migración de HU-13 (documento 28), ya aplicada a `nexit_dev`. |
| — | [`schema/11_presencia_usuarios.sql`](schema/11_presencia_usuarios.sql) | **Sí se ejecuta, una vez, sin orden fijo.** Agrega `ultima_actividad` a `usuarios` — la parte de producción de la migración de HU-12 (documento 29), ya aplicada a `nexit_dev`. |
| — | [`schema/12_estado_contrasena_usuarios.sql`](schema/12_estado_contrasena_usuarios.sql) | **Sí se ejecuta, una vez, sin orden fijo.** Agrega `contrasena_configurada` a `usuarios` — la parte de producción de la migración de la detección "primera vez vs. recurrente" (documento 30), ya aplicada a `nexit_dev`. |
| — | [`schema/13_bucket_adjuntos_proveedores.sql`](schema/13_bucket_adjuntos_proveedores.sql) | **Sí se ejecuta, una vez por proyecto de Supabase, sin orden fijo.** UPSERT idempotente que crea/confirma la configuración del bucket `adjuntos-proveedores` (privado, 20 MB, whitelist de MIME) directo en `storage.buckets` — antes solo existía como pasos manuales en el dashboard, sin quedar en ningún script (documento 28, HU-13). |
| — | [`erd.png`](erd.png) | Diagrama entidad-relación del modelo de datos. |
| — | [`schema/referencia/nexus_schema_v2.sql`](schema/referencia/nexus_schema_v2.sql) | **No se ejecuta — solo consulta.** Documento de referencia legible del diseño completo del esquema (tablas, triggers, Row Level Security), incluido el modelo de permisos de 4 niveles y solicitudes_eliminacion (novena revisión). Quedó desactualizado frente a las migraciones reales (ver la advertencia en su encabezado y el documento 09), por eso se sacó de `docs/schema/` a esta subcarpeta aparte — así ningún archivo de la carpeta principal es ambiguo sobre si se ejecuta o no. |

## Convención para documentos futuros

Cuando se agregue un documento nuevo de una fase o revisión importante, se numera siguiendo el consecutivo (`10-...`, `11-...`) y se agrega una fila a esta tabla. Evita crear carpetas nuevas para "una sesión" o "una herramienta" (como pasó con `superpowers/`, que ya se reorganizó dentro de esta numeración) — todo documento de proyecto vive directamente en `docs/`, plano, numerado.
